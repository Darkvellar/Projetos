let qualquer = 'Legal';
console.log(qualquer);
console.log(typeof qualquer);

// Evite usar nomes genéricos ou siglas para variáveis
// Prefira nomes que expliquem claramente o propósito da variável

qualquer = 3.1516;
console.log(qualquer);
console.log(typeof qualquer);

// Evitar nomes genéricos e siglas - exemplos negativos
let valor = ''; // Nome genérico, não explica o propósito
let numero = 1; // Nome genérico, poderia ser mais descritivo
let pqp = false; // Produto Químico Perigoso? Nome confuso e inadequado

/* Prefira código claro a comentários excessivos
   Evite comentários óbvios, como:
   let soma = a + b; // Soma a e b
   Comentários são bons, mas não exagere. Use-os para explicar o "porquê", não o "como". */
